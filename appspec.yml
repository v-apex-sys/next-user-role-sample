version: 0.0
os: linux
files:
  - source: ./.next
  # 各ファイルのパスが通るように
    destination: /var/www/html/rentals/_next
file_exists_behavior: OVERWRITE
permissions:
  - object: /var/www/html
    owner: ubuntu
    group: ubuntu
    mode: 777
    pattern: "**"
hooks:
  # ApplicationStop:
  #   - location: scripts/stop_server.sh
  #     timeout: 300
  #     runas: ubuntu
  # BeforeInstall:
  #   - location: scripts/before_install.sh
  #     timeout: 300
  #     runas: ubuntu
  # AfterInstall:
  #   - location: scripts/after_install.sh
  #     timeout: 300
  #     runas: ubuntu
  # ApplicationStart:
  #   - location: scripts/start_server.sh
  #     timeout: 300
  #     runas: ubuntu
